spring.application.name=catalogo
# URL de conex�o (troque 'db_catalogo' pelo nome que voc� criou no pgAdmin)
spring.datasource.url=jdbc:postgresql://localhost:5432/catalogo
spring.datasource.username=postgres
spring.datasource.password=123456
# Driver e Dialeto
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Hibernate: 'update' cria as tabelas automaticamente se n�o existirem
spring.jpa.hibernate.ddl-auto=update

# Opcional: Mostra o SQL no console para voc� ver o que est� acontecendo
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
